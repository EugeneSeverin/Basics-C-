// Precedence and expressions

// Precedence

// () - 3 (highest)
// *, /, % - 2 (middle)
// +, - - 1 (lowest) 

// area of triangle: a = (b * h)/2 OR 0.5 * (b * h)

// perimeter of a rectangle: p = 2 * (l + b)

// root of Quadratic Eq: 
//                      # include <cmath>
//                      r = (-b + sqrt(b*b - 4 * a * c))/(2*a)

// speed: S = (v*v - u*u) / (2*a) OR S = (pov(v,2) - pov(u,2)) / (2*a)